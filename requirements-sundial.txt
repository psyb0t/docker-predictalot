# Sundial sidecar deps. Installed into /opt/sundial-venv only.
# Torch, numpy, and huggingface_hub are symlinked from /opt/venv (the main
# venv) so they're NOT listed here — adding them would duplicate ~2GB.
#
# Manually pinned (not hash-locked yet). Bump versions intentionally; sundial
# is fragile to transformers >= 4.42 (DynamicCache internals removed).
transformers==4.40.1
tokenizers==0.19.1
regex==2024.5.15
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4
starlette==0.51.0
anyio==4.13.0
sniffio==1.3.1
h11==0.14.0
click==8.1.7
